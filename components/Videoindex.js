//import React, {useEffect, useRef} from 'react'
/*
type Props = {
    url: string
}

export const VideoIndex: React.FC<Props> = ({url}) => {

    const videoRef = useRef()

    useEffect(() => {
        setTimeout(() => {
            videoRef.current.play()
            }, 5000) 
        }, [])

    return(
        <div className="video-index">
            <video ref={videoRef} width="1920" loop muted style={{...}}>
                <source src={url} type="video/mp4"/>
            </video>
        </div>
    )
}

*/